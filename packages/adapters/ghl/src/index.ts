/**
 * @donna/adapter-ghl
 *
 * GoHighLevel (LeadConnector) CRM `CapabilityAdapter` (Technical Plan §7) — a
 * concrete integration on top of `@donna/adapter-http`. It maps DONNA's CRM
 * operations to GHL v2 API calls; the token is a secret from the environment.
 */
export * from './ghl-adapter.js';
