{
  "name": "@donna/context",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Context Packet builder for DONNA V2 — budgeted, cache-ordered working context.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  }
}
